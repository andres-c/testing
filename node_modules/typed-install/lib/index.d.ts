export interface MainOpts {
    dev?: boolean;
    prod?: boolean;
    yarn?: boolean;
    exact?: boolean;
}
declare const _default: (modules: string[], { dev, prod, yarn, exact }?: MainOpts, shouldSpin?: boolean) => Promise<void>;
export default _default;
